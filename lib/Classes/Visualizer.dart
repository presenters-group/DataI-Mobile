import 'package:flutter/cupertino.dart';

class Visualizer {
  bool isBar = true;
  bool isLine = false;
  bool isPie = false;

  Visualizer(
      {@required this.isBar, @required this.isLine, @required this.isPie});
}
